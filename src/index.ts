import { maximumNumber, minimumNumber, probability } from "./math"
import { generateOTP, randomAdjective, randomAnimal, randomElement, randomName, randomNumber } from "./random"
import { wait } from "./time"
import { retry } from "./utility"
export { generateOTP, maximumNumber, minimumNumber, probability, randomAdjective, randomAnimal, randomElement, randomName, randomNumber, retry, wait }